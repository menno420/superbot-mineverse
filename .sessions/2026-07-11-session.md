# Session 2026-07-11 — ORDER 000 stage-1 walking skeleton

> **Status:** `complete`

## Plan

Execute ORDER 000 (coordinator-assigned): the read-only stage-1 walking
skeleton — a committed sample snapshot using oracle-true field names
(researched from menno420/superbot `disbot/services/mining_workflow.py` and
`disbot/utils/` via raw fetches), a stdlib Python backend serving
`GET /api/snapshot` plus the static frontend, pytest coverage, the adopt-time
interview slots filled so substrate-gate can report, shipped as a READY PR on
`claude/order-000-walking-skeleton` with auto-merge (squash) armed.

Constraints honored: no auth, no database, no secrets, no live data, no write
path; `control/status.md` and `control/inbox.md` untouched (single-writer
files owned by the coordinator/manager).

## Close-out

- Shipped the stage-1 skeleton: `data/sample_snapshot.json` (5 miners,
  oracle-true field names), `server/app.py` (stdlib, `GET /api/snapshot` +
  static `web/`), `web/` frontend (miner cards, depth race, leaderboard,
  honest error states), `tests/` (11 pytest cases).
- verify: `python3 -m pytest -q` → 11 passed. Live smoke: served `/`,
  `/api/snapshot`, `/style.css` locally, all 200.
- Known red: `check --strict` exits 1 on `[status-no-heartbeat]` for the
  seeded `control/status.md`. This session was instructed NOT to touch
  `control/status.md` (coordinator-owned single-writer file), so
  substrate-gate stays red until the status.md owner lands a real heartbeat.
- PR #2 merged to main at 2026-07-11T01:43:09Z (squash 36788b2); substrate-gate
  green on the PR (run 29135124796) and on main (run 29135130743) — the
  coordinator's heartbeat (#1) had landed on base, clearing the seed-status red
  this card earlier flagged. This close-out commit flips the card `complete`.

## Close-out (evidence)

**Evidence (auto-collected, verified):**

- code touched (2): `server/app.py`, `web/app.js`
- docs touched (8): `docs/AGENT_ORIENTATION.md`, `docs/ai-project-workflow.md`, `docs/architecture.md`, `docs/collaboration-model.md`, `docs/current-state.md`, `docs/owner-profile.md`, `docs/ownership.md`, `docs/runtime_contracts.md`
- other touched (7 committed): `.claude/CLAUDE.md`, `.gitignore`, `CONSTITUTION.md`, `README.md`, `data/sample_snapshot.json`, `web/index.html`, `web/style.css` (`control/status.md` was read/restored only — NOT committed; it is the coordinator's single-writer file)
- sessions touched (1): `.sessions/2026-07-11-session.md`
- tests touched (2): `tests/test_api.py`, `tests/test_snapshot.py`
- git: branch `claude/order-000-walking-skeleton`, HEAD d1d8c9f04 → 11c112db5 + this commit.
- verify: `python3 -m pytest -q` → `11 passed` (plus live smoke of `/`, `/api/snapshot`, `/style.css` — all 200)

**Judgment:**

- Decisions made: stdlib `http.server` over FastAPI (no dependency exists in the environment or CI; the gate never installs packages); interview slots answered from lived repo facts so substrate-gate's UNRENDERED findings clear; `control/status.md` left untouched per coordinator single-writer instruction even though `check --strict` reds on its seed heartbeat.
- Next session should know: pick up at PR #2 — everything is green except `[status-no-heartbeat]` on the seeded `control/status.md`; the status.md owner must land a real heartbeat (branch or main + branch update) for substrate-gate to pass.

## 💡 Session idea

Ship a `snapshot_contract.py` (stage 2) that validates any snapshot against the
oracle field names pinned in `tests/test_snapshot.py::REQUIRED_MINER_FIELDS` —
then the live exporter on the superbot side can import the same constant and
structural drift becomes a test failure instead of a runtime surprise.

## ⟲ Previous-session review

First real session on this repo — the adopt seed was clean, but the seeded
`control/status.md` heartbeat reds the strict gate for any PR from a lane that
is forbidden to write it; workflow improvement: the coordinator should land the
first heartbeat immediately after adopt so build lanes never inherit that red.

- **📊 Model:** claude · high effort · build (walking-skeleton implementation)
